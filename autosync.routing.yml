autosync.config_controller_generateTar:
  path: '/autosync/generateTar'
  defaults:
    _controller: '\Drupal\autosync\Controller\ConfigController::generateTar'
    _title: 'autosync Title'
  requirements:
    _permission: 'access content'

autosync.config_controller_applyConfig:
  path: '/autosync/applyConfig'
  defaults:
    _controller: '\Drupal\autosync\Controller\ConfigController::applyConfig'
    _title: 'autosync Title'
  requirements:
    _permission: 'access content'

autosync.config_set_form:
  path: '/admin/config/development/autosync/ConfigSetForm'
  defaults:
    _form: '\Drupal\autosync\Form\ConfigSetForm'
    _title: 'ConfigSetForm'
  requirements:
    _permission: 'access administration pages'
